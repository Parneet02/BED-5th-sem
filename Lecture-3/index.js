const express = require("express") //to require packages, if we use node modules packages we have to give only name and if we using any ither then write full path
//console.log(express)
module.exports=require("./lib/sum")